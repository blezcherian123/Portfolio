ParticleNetworkPreview.jsx — a live preview showing the background behind sample hero text, so you can see how it'll look in your portfolio.
ParticleNetworkBackground.jsx — the reusable component itself, ready to drop into your project.

